var minhaVariavel = "Empresa Procenge";
function somaNumeros(a, b) {
    return a + b;
}
var num = 0;
var pi = 3.14;
var numeros = [1, 2, 3];
numeros.map(function (valor) {
    return valor * 2;
});
numeros.map(function (valor) { return valor * 2; });
var math = /** @class */ (function () {
    function math() {
    }
    math.prototype.soma = function (x, y) {
        return x + y;
    };
    return math;
}());
