var minhaVariavel = "Empresa Procenge";

function somaNumeros1( a, b) {
    return a + b;
}

let num1 = 0;

const pi1 = 3.14;

var numeros = [1, 2, 3];

numeros.map(function(valor) {
    return valor * 2;
})

numeros.map(valor => valor * 2);

class math1 {
    soma(x, y) {
        return x + y;
    }
}